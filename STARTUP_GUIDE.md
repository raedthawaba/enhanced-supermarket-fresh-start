# دليل إصلاح مشاكل تشغيل التطبيق

## المشكلة: الصفحة البيضاء أو عدم تشغيل التطبيق

تم تطبيق عدة إصلاحات لحل هذه المشكلة:

### 🔧 الإصلاحات المطبقة:

#### 1. **إصلاح قاعدة البيانات**
- إضافة `sqflite_common_ffi` للويب
- معالجة أخطاء قاعدة البيانات

#### 2. **إصلاح تدفق التطبيق**
- إضافة error handling في main()
- إضافة navigation من splash screen
- تحسين AuthBloc state management

#### 3. **إضافة Fallback App**
- في حالة فشل التطبيق الرئيسي، سيتم تشغيل `TestApp` البسيط
- للتأكد من أن Flutter يعمل على الأقل

### 🚀 خطوات التشغيل:

#### **الخطوة 1: تحديث Dependencies**
```bash
flutter pub get
```

#### **الخطوة 2: تنظيف Cache (اختياري)**
```bash
flutter clean
flutter pub get
```

#### **الخطوة 3: تشغيل التطبيق**
```bash
flutter run
```

### 🎯 ما يجب أن يحدث الآن:

1. **Splash Screen**: ستظهر شاشة تحميل جميلة مع شعار المتجر
2. **Auto Navigation**: بعد 3 ثوانٍ، سيتم الانتقال للشاشة الرئيسية
3. **Authentication**: إذا لم يكن هناك مستخدم مسجل دخول، ستظهر شاشة تسجيل الدخول
4. **Home Screen**: إذا كان المستخدم مسجل دخول، ستظهر الصفحة الرئيسية

### 🔍 في حالة استمرار المشكلة:

#### **الخيار 1: فحص Console**
- افتح Developer Tools في المتصفح (F12)
- راجع Console للأخطاء
- أبلغني بأي رسائل خطأ

#### **الخيار 2: اختبار بسيط**
- التطبيق يحتوي على `TestApp` كـ fallback
- إذا ظهرت صفحة بسيطة بأيقونة سلة تسوق، فهذا يعني أن Flutter يعمل
- المشكلة في التهيئة أو الـ routing

#### **الخيار 3: إعادة تشغيل**
```bash
# تنظيف كامل وإعادة بناء
flutter clean
flutter pub get
flutter run --web-renderer html
```

### 📱 بيانات تسجيل الدخول للاختبار:
- **Email**: admin@example.com
- **Password**: 123456

### 🎨 ميزات التطبيق:
- ✅ تسجيل الدخول والتسجيل
- ✅ عرض المنتجات
- ✅ الفئات والمنتجات المميزة
- ✅ السلة والمفضلة
- ✅ الطلبات والملف الشخصي
- ✅ البحث والإشعارات
- ✅ دعم اللغة العربية

### 🔧 استكشاف الأخطاء:

#### **خطأ قاعدة البيانات**:
```
DartError: databaseFactory not initialized
```
✅ **تم إصلاحه**: sqflite_common_ffi مضاف

#### **صفحة بيضاء**:
✅ **تم إصلاحه**: error handling وإضافة fallback

#### **مشاكل في AuthBloc**:
✅ **تم إصلاحه**: routing وstate management محدث

### 📞 إذا استمرت المشاكل:

1. **تأكد من إصدار Flutter**: `flutter --version`
2. **تأكد من دعم الويب**: `flutter doctor`
3. **اختبر متصفح آخر**: Chrome, Firefox, Edge
4. **امسح cache المتصفح**: Ctrl+F5 أو Cmd+Shift+R

**التطبيق الآن يجب أن يعمل بشكل مثالي! 🎉**